# archivist
A domain-driven MCP interface to the Second Brain. It abstracts retrieval strategies behind a stable set of domain capabilities, allowing agents to access knowledge without being coupled to storage or retrieval implementations.
